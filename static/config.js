/*
   database: The Auth0 Database Connection that should be used to sign in users by email and password (using a database name)
*/
var config = {
    domain: "",
    clientId: "",
    redirectUri: "http://localhost:3000",
    database: "Username-Password-Authentication"
}